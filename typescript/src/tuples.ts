// tuple - thought of as an array with a fixed number of elements.

let user: [number, string] = [7, "JamesBond"]; // a tuple

user.push(3) // however pushing element to tuple this way seems working. 

// Best Practice is use tuples to store only two values
// like key-value pairs
 

